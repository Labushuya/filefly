<?xml version="1.0" encoding="utf-8"?>
<!-- allowBackup=false + explizite Regeln: KEINE App-Daten in Cloud-Backup/Transfer.
     Der JWT-Token liegt in DataStore und darf das Gerät nicht verlassen. -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
    </device-transfer>
</data-extraction-rules>
